import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'http-test-ng',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
